import template from './betslip.html';
import controller from './betslip.controller';
import './betslip.scss';

let betslipComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default betslipComponent;
